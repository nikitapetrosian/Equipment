export interface IbuildingAndRooms {
    id: string;
    name: string;
    parts: string[];
}
export interface Iequipment {
    id: string;
    name: string;
    count: number;
    placeId: string;

}